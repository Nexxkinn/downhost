import { de, grab, DownMeta, DownRequest, DownType, DownPagesRequest, DownMetaArgs } from "./_deps.ts";

const token = de("");
const srvc  = de("");

export async function metadata({link}:DownMetaArgs): Promise<DownMeta> {
    throw new Error('template')
}



