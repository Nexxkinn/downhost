<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <base href="/" target="_blank">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script type="module" src="static/fast-components.min.js"></script>
    <script src="static/index.js"></script>
    <title>{@title}</title>
    <style>
        :root {
            --background-color: #181818;
            --navbar-height: 94px;
            --section-height-spacing: 100px 0px;
            --section-height-spacing-mobile: 50px 0px;
        }

        html,
        body {
            background-color: var(--background-color);
            font-family: SegoeUIVF, Segoe UI, SegoeUI, Helvetica Neue, Helvetica, Arial, sans-serif;
        }

        .form {
            display: inline-flex;
            place-items: flex-end;
        }

        .itemname {
            text-overflow: ellipsis;
            overflow: hidden;
            white-space: nowrap;
            text-overflow: ellipsis;
        }
    </style>
</head>

<body>

    <fast-design-system-provider use-defaults>
        <div class="form">
            <fast-text-field appearance="outline" placeholder="https://" autofocus id="url">Downhost
            </fast-text-field>
            <fast-button id="submit">Submit</fast-button>
        </div>
        <div>
            <fast-accordion id="list">
            </fast-accordion>
        </div>
    </fast-design-system-provider>
</body>

</html>