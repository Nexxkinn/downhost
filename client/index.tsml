<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <base href="/" target="_blank">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" sizes="128x128" href="static/icon.png">
    <!-- <script type="module" src="https://unpkg.com/@microsoft/fast-components"></script> -->
    <script type="module" src="static/fast-components.min.js"></script>
    <script src="static/index.js"></script>
    <title>{@title}</title>
    <style>
        :root {
            --background-color: #181818;
            --navbar-height: 94px;
            --section-height-spacing: 100px 0px;
            --section-height-spacing-mobile: 50px 0px;
        }

        html,
        body {
            background-color: var(--background-color);
            font-family: SegoeUIVF, Segoe UI, SegoeUI, Helvetica Neue, Helvetica, Arial, sans-serif;
            max-width: 842px;
            margin: 0 auto;
            padding: 1em;
        }
        
        .header {
            display: inline-flex;
            width: 100%;
        }

        .header > .nav {
            margin: auto 0 auto auto;
        }

        .form {
            width: 100%;
            display: inline-flex;
            place-items: flex-end;
            margin-bottom: 12px;
        }
        .form > #field {
            width: 100%;
        }
        .form > #submit {
            width: 96px;
            margin-left: 1em;
        }

        .content {
            width: 100%;
            display: flex;
            flex-direction: row;
        }
        .content[hidden] {
            display: none;
        }

        .desc {
            margin: auto 0 auto 12px;
            width: 100%;
        }

        .lib-list {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
        }

        .lib-list fast-card {
            display: grid;
            width: 150px;
        }

        .lib-list .option {
            border-radius:unset;
            grid-area: opt;
            width:50%;
        }

        .lib-list .title {
            grid-area: title;
            font-weight: 500;
            font-size: 14px;
            width: 130px;
            text-overflow: ellipsis;
            overflow: hidden;
            padding: 10px 10px 0 10px;
            background: var(--neutral-fill-card-rest);
            display: -webkit-box;
            -webkit-box-orient: vertical; 
            -webkit-line-clamp: 2;
        }

        .catalog-list {
            display: flex;
            flex-direction: column;
            flex-wrap: wrap;
            gap: 8px;
        }

        .nav {
            display: flex;
        }

        .nav-button {
            flex-grow: 1;
            width: 50%;
        }

        .settings-dialog {
            padding: 0 14px;
        }

        @media only screen and (min-width: 600px) {
            .nav {
                display: unset;
            }

            .nav-button {
                flex-grow: unset;
                width: unset;
                min-width: 80px;
            }
        }

        /* desktop or pointer-supported devices */
        @media (hover: hover) {
            .lib-list .option {
                display: none;
            }

            .lib-list fast-card {
                grid-auto-rows: 190px 60px;
            }
            
            .lib-list fast-card:hover {
                grid-auto-rows: 150px 100px;
            }

            .lib-list fast-card:hover .option {
                display: inline-grid;
            }
        }

        /* smartphones or touch-supported devices */
        @media (hover: none) {
            .lib-list fast-card {
                grid-auto-rows: 150px 100px;
            }
        }
    </style>
</head>

<body>

    <fast-design-system-provider use-defaults>
        <div class="header">
            <h1>DownHost</h1>
            <div class="nav">
                <fast-button id="settings"></fast-button>
            </div>
        </div>
        <div class="form">
            <fast-text-field appearance="outline" placeholder="Search or put gallery link here..." autofocus id="field">
            </fast-text-field>
            <fast-button id="submit" hidden>Download</fast-button>
        </div>
        <div id="nav" class="nav">
            <fast-button class="nav-button" id="catalogTab" appearance="accent" >Downloads</fast-button>
            <fast-button class="nav-button" id="libraryTab" appearance="stealth">Library</fast-button>
        </div>
        <fast-divider></fast-divider>
        <div id="panel">
            <div id="catalogPanel" hidden>
                <div id="cataloglist" class="catalog-list" ></div>
            </div>
            <div id="libraryPanel" hidden>
                <div id="liblist" class="lib-list"></div>
            </div>
        </div>
        </div>
        <fast-dialog id="settings-dialog" modal hidden>
            <div class="settings-dialog">
                <h2>Settings</h2>
                <span>To be added later.</span> <br/>
                <fast-button id="dialog-close">Close</fast-button>
                <h2>About</h2>
                <div>DownHost v. 0.0.1</div>
            </div>
        </fast-dialog>
    </fast-design-system-provider>
</body>

</html>